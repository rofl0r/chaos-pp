<?xml version="1.0" encoding="UTF-8"?>
<header name="chaos/preprocessor/highprec/sign.h" author="Paul Mensonides" date="2003-2004">
<overview>
	The <self/> header defines a macro that extracts the sign of a <link id="high-precision value"/>.
</overview>

<macro id="SIGN_HP" group="sign">
	<abstract>
		The <SIGN_HP/> macro extracts the sign of a <link id="high-precision value"/>.
	</abstract>
	<usage>
		<syntax><SIGN_HP/>(x)</syntax>
		<param id="x">
			The <link id="high-precision value"/> whose sign is to be extracted.
		</param>
	</usage>
	<para>
		If <var>x</var> is a <link id="high-precision value"/> representing a negative number, this macro expands to <code>1</code>.
		Otherwise, it expands to <code>0</code>.
	</para>
	<listing>
		#include <chaos-preprocessor-highprec-literal.h/>
		#include <chaos-preprocessor-highprec-sign.h/>
		
		<SIGN_HP/>(<LITERAL_HP/>(1 2 3)) // 0
		<SIGN_HP/>(<LITERAL_HP/>((-)1))  // 1
	</listing>
	<derivative id="SIGN_HP_ID"/>
	<derivative id="SIGN_HP_" lang="C99"/>
</macro>

</header>
