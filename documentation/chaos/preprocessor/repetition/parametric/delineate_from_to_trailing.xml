<?xml version="1.0" encoding="UTF-8"?>
<header name="chaos/preprocessor/repetition/parametric/delineate_from_to_trailing.h" author="Paul Mensonides" date="2003-2004">
<overview>
	The <self/> header defines an algorithm that delineates a user-specified target for each number in a specified range which can be <link id="parametrically resumed"/>.
</overview>

<macro id="DELINEATE_FROM_TO_TRAILING_PARAMETRIC" type="indirect" group="delineation">
	<abstract>
		The <DELINEATE_FROM_TO_TRAILING_PARAMETRIC/> macro delineates its fourth argument for each number in the range specified by its first two arguments.
	</abstract>
	<usage lang="C99">
		<syntax><EXPR/>(<DELINEATE_FROM_TO_TRAILING_PARAMETRIC/>(from, to, sep, macro))</syntax>
		<param id="from">
			The lower bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <code>0</code> to <var>to</var>.
		</param>
		<param id="to">
			The upper bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <var>from</var> to <LIMIT_MAG/>.
		</param>
		<param id="sep">
			The delineator to be situated between each repetition of <var>macro</var>.
			This delineator must be a macro name that refers to a macro with a signature compatible with the following pseudo-signature:
			<snippet>
				sep()
			</snippet>
		</param>
		<param id="macro">
			The target that is to be repeated.
			This target must be either a <link id="λ expression"/> or a macro name that refers to a macro with a signature compatible with the following pseudo-signature:
			<snippet>
				macro(s, n)
			</snippet>
		</param>
	</usage>
	<usage lang="C99">
		<syntax><EXPR/>(<DELINEATE_FROM_TO_TRAILING_PARAMETRIC/>(from, to, sep, macro, ...))</syntax>
		<param id="from">
			The lower bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <code>0</code> to <var>to</var>.
		</param>
		<param id="to">
			The upper bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <var>from</var> to <LIMIT_MAG/>.
		</param>
		<param id="sep">
			The delineator to be situated between each repetition of <var>macro</var>.
			This delineator must be a macro name that refers to a macro with a signature compatible with the following pseudo-signature:
			<snippet>
				sep()
			</snippet>
		</param>
		<param id="macro">
			<para>
				The target that is to be repeated.
				This target must be either a <link id="λ expression"/> or a macro name that refers to a macro with a signature compatible with the following pseudo-signature:
				<snippet>
					macro(s, n, data<sup>†</sup>)
				</snippet>
			</para>
			<para>
				<sup>†</sup>
				The <var>data</var> pseudo-parameter must be either <code>...</code> or the number of parameters that corresponds to the number of arguments in the auxiliary data, <code>__VA_ARGS__</code>.
			</para>
		</param>
		<param id="..." optional="1">
			Auxiliary data to be passed through the algorithm to the target.
		</param>
	</usage>
	<usage lang="C/C++">
		<syntax><EXPR/>(<DELINEATE_FROM_TO_TRAILING_PARAMETRIC/>(from, to, sep, macro, data))</syntax>
		<param id="from">
			The lower bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <code>0</code> to <var>to</var>.
		</param>
		<param id="to">
			The upper bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <var>from</var> to <LIMIT_MAG/>.
		</param>
		<param id="sep">
			The delineator to be situated between each repetition of <var>macro</var>.
			This delineator must be a macro name that refers to a macro with a signature compatible with the following pseudo-signature:
			<snippet>
				sep()
			</snippet>
		</param>
		<param id="macro">
			The target that is to be repeated.
			This target must be a macro name that refers to a macro with a signature compatible with the following pseudo-signature:
			<snippet>
				macro(s, n, data)
			</snippet>
		</param>
		<param id="data">
			Auxiliary data to be passed through the algorithm to the target.
		</param>
	</usage>
	<para>
		This macro behaves identically to <DELINEATE_FROM_TO_PARAMETRIC/> except that it inserts a leading delineator if any repetitions exist.
	</para>
	<para>
		This macro deduces the current <link id="recursion state"/> and invokes <DELINEATE_FROM_TO_TRAILING_PARAMETRIC_S/>.
		If the current <link id="recursion state"/> is already known, direct use of <DELINEATE_FROM_TO_TRAILING_PARAMETRIC_S/> is more efficient.
	</para>
	<para>
		This macro may not be used if <link id="bypass semantics"/> are in effect.
	</para>
	<derivative id="DELINEATE_FROM_TO_TRAILING_PARAMETRIC_ID"/>
	<derivative id="DELINEATE_FROM_TO_TRAILING_PARAMETRIC_" lang="C99"/>
	<derivative id="DELINEATE_FROM_TO_TRAILING_PARAMETRIC_S">
		<usage lang="C99">
			<syntax><EXPR_S/>(s)(<DELINEATE_FROM_TO_TRAILING_PARAMETRIC_S/>(s, from, to, sep, macro))</syntax>
		</usage>
		<usage lang="C99">
			<syntax><EXPR_S/>(s)(<DELINEATE_FROM_TO_TRAILING_PARAMETRIC_S/>(s, from, to, sep, macro, ...))</syntax>
		</usage>
		<usage lang="C/C++">
			<syntax><EXPR_S/>(s)(<DELINEATE_FROM_TO_TRAILING_PARAMETRIC_S/>(s, from, to, sep, macro, data))</syntax>
		</usage>
		<derivative id="DELINEATE_FROM_TO_TRAILING_PARAMETRIC_S_ID"/>
		<derivative id="DELINEATE_FROM_TO_TRAILING_PARAMETRIC_S_" lang="C99"/>
	</derivative>
</macro>

</header>
