<?xml version="1.0" encoding="UTF-8"?>
<header name="chaos/preprocessor/algorithm/rest_n.h" author="Paul Mensonides" date="2003-2004">
<overview>
	The <self/> header defines an algorithm that yields the remainder of a <link id="generic data structure"/> after removing a user-specified number of elements.
</overview>

<macro id="REST_N" type="indirect" group="range">
	<abstract>
		The <REST_N/> macro removes the number of elements specified by its first argument from the head of its second argument.
	</abstract>
	<usage>
		<syntax><REST_N/>(n, g)</syntax>
		<param id="n">
			The number of elements to be removed.
			Valid values range from <code>0</code> to <LIMIT_MAG/>.
		</param>
		<param id="g">
			The <link id="generic data structure"/> from which elements are to be removed.
		</param>
	</usage>
	<para>
		This macro expands to a <link id="generic data structure"/> of the same type as <var>g</var> containing all but the first <var>n</var> elements of <var>g</var>.
		Consequently, <var>g</var> must contain at least <var>n</var> elements.
	</para>
	<para>
		The running time of <REST_N/> can be described in terms of the number of <link id="recursion steps"/> required to complete the algorithm.
		The asymptotic upper and lower bounds are Θ(<x-var>n</x-var>+1), where <x-var>n</x-var> is <var>n</var>.
	</para>
	<para>
		This macro uses <link id="bypass semantics"/>.
		If <link id="bypass semantics"/> are already in effect, <REST_N_BYPASS/> must be used instead.
	</para>
	<listing>
		#include <chaos-preprocessor-algorithm-rest_n.h/>
		#include <chaos-preprocessor-list-core.h/>
		
		<REST_N/>(
		    3,
		    (<LIST/>) (a, (b, (c, (d, (e, ...)))))
		)
		// (<LIST/>) (d, (e, ...))
	</listing>
	<derivative id="REST_N_ID"/>
	<derivative id="REST_N_" lang="C99"/>
	<derivative id="REST_N_BYPASS">
		<usage>
			<syntax><REST_N_BYPASS/>(s, n, g)</syntax>
		</usage>
		<derivative id="REST_N_BYPASS_ID"/>
		<derivative id="REST_N_BYPASS_" lang="C99"/>
	</derivative>
</macro>

</header>
