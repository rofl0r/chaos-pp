<?xml version="1.0" encoding="UTF-8"?>
<header name="chaos/preprocessor/highprec/less.h" author="Paul Mensonides" date="2003-2004">
<overview>
	The <self/> header defines a <link id="high-precision"/> "less-than" operator.
</overview>

<macro id="LESS_HP" group="less">
	<abstract>
		The <LESS_HP/> macro tests whether its first operand is less than its second operand.
	</abstract>
	<usage>
		<syntax><LESS_HP/>(x, y)</syntax>
		<param id="x">
			The first operand.
		</param>
		<param id="y">
			The second operand.
		</param>
	</usage>
	<para>
		If <var>x</var> is less than <var>y</var>, this macro expands to <code>1</code>.
		Otherwise, it expands to <code>0</code>.
	</para>
	<listing>
		#include <chaos-preprocessor-highprec-less.h/>
		#include <chaos-preprocessor-highprec-literal.h/>
		
		#define X() <LITERAL_HP/>(1 2 3)
		#define Y() <LITERAL_HP/>(4 5)
		
		<LESS_HP/>(X(), Y()) // 0
		<LESS_HP/>(X(), Y()) // 1
	</listing>
	<derivative id="LESS_HP_ID"/>
	<derivative id="LESS_HP_" lang="C99"/>
</macro>

</header>
