<?xml version="1.0" encoding="UTF-8"?>
<header name="chaos/preprocessor/repetition/parametric/repeat_from_to.h" author="Paul Mensonides" date="2003-2004">
<overview>
	The <self/> header defines an algorithm that repeats a user-specified target for each number in a specified range which can be <link id="parametrically resumed"/>.
</overview>

<macro id="REPEAT_FROM_TO_PARAMETRIC" type="indirect" group="repetition">
	<overview>
		The <REPEAT_FROM_TO_PARAMETRIC/> macro repeats its third argument for each number in the range specified by its first two arguments.
	</overview>
	<usage lang="C99">
		<syntax><EXPR/>(<REPEAT_FROM_TO_PARAMETRIC/>(from, to, macro))</syntax>
		<param id="from">
			The lower bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <code>0</code> to <var>to</var>.
		</param>
		<param id="to">
			The upper bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <var>from</var> to <LIMIT_MAG/>.
		</param>
		<param id="macro">
			The target that is to be repeated.
			This target must be either a <link id="λ expression"/> or a macro name that refers to a macro with a signature compatible with the following pseudo-signature:
			<snippet>
				macro(s, n)
			</snippet>
		</param>
	</usage>
	<usage lang="C99">
		<syntax><EXPR/>(<REPEAT_FROM_TO_PARAMETRIC/>(from, to, macro, ...))</syntax>
		<param id="from">
			The lower bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <code>0</code> to <var>to</var>.
		</param>
		<param id="to">
			The upper bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <var>from</var> to <LIMIT_MAG/>.
		</param>
		<param id="macro">
			<para>
				The target that is to be repeated.
				This target must be either a <link id="λ expression"/> or a macro name that refers to a macro with a signature compatible with the following pseudo-signature:
				<snippet>
					macro(s, n, data<sup>†</sup>)
				</snippet>
			</para>
			<para>
				<sup>†</sup>
				The <var>data</var> pseudo-parameter must be either <code>...</code> or the number of parameters that corresponds to the number of arguments in the auxiliary data, <code>__VA_ARGS__</code>.
			</para>
		</param>
		<param id="..." optional="1">
			Auxiliary data to be passed through the algorithm to the target.
		</param>
	</usage>
	<usage lang="C/C++">
		<syntax><EXPR/>(<REPEAT_FROM_TO_PARAMETRIC/>(from, to, macro, data))</syntax>
		<param id="from">
			The lower bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <code>0</code> to <var>to</var>.
		</param>
		<param id="to">
			The upper bound of the range of numbers for which <var>macro</var> is to be repeated.
			Valid values range from <var>from</var> to <LIMIT_MAG/>.
		</param>
		<param id="macro">
			The target that is to be repeated.
			This target must be a macro name that refers to a macro with a signature compatible with the following pseudo-signature:
			<snippet>
				macro(s, n, data)
			</snippet>
		</param>
		<param id="data">
			Auxiliary data to be passed through the algorithm to the target.
		</param>
	</usage>
	<para>
		This macro behaves identically to <REPEAT_FROM_TO/> except that the algorithm itself yields a trampolined <link id="deferred expression"/> whenever the number of <link id="recursion steps"/> is exhausted.
		This allows for <link id="parametric resumption"/>.
	</para>
	<para>
		Ignoring the complexity of <var>macro</var> (which is invoked exactly <var>to</var> - <var>from</var> times), the running time of <REPEAT_FROM_TO_PARAMETRIC/> can be described in terms of the number of <link id="recursion steps"/> required to complete the algorithm.
		The asymptotic upper and lower bounds are Θ(<x-var>t</x-var>-<x-var>f</x-var>+1), where <x-var>t</x-var> is <var>to</var> and <x-var>f</x-var> is <var>from</var>.
		These bounds preserve all low-order terms, which implies that the algorithm can repeat the target <x-var>p</x-var>∙(<x-var>s<sub>ω</sub></x-var>-<x-var>s</x-var>-1) times, where <x-var>p</x-var> is the number of <link id="parametric resumptions"/> plus the initial entry, <x-var>s<sub>ω</sub></x-var> is <LIMIT_EXPR/>, and <x-var>s</x-var> is the original <link id="recursion state"/> at the beginning of the algorithm (i.e. <code><STATE/>()</code>).
	</para>
	<para>
		This macro deduces the current <link id="recursion state"/> and invokes <REPEAT_FROM_TO_PARAMETRIC_S/>.
		If the current <link id="recursion state"/> is already known, direct use of <REPEAT_FROM_TO_PARAMETRIC_S/> is more efficient.
	</para>
	<para>
		This macro may not be used if <link id="bypass semantics"/> are in effect.
	</para>
	<derivative id="REPEAT_FROM_TO_PARAMETRIC_ID"/>
	<derivative id="REPEAT_FROM_TO_PARAMETRIC_" lang="C99"/>
	<derivative id="REPEAT_FROM_TO_PARAMETRIC_S">
		<usage lang="C99">
			<syntax><EXPR_S/>(s)(<REPEAT_FROM_TO_PARAMETRIC_S/>(s, from, to, macro))</syntax>
		</usage>
		<usage lang="C99">
			<syntax><EXPR_S/>(s)(<REPEAT_FROM_TO_PARAMETRIC_S/>(s, from, to, macro, ...))</syntax>
		</usage>
		<usage lang="C/C++">
			<syntax><EXPR_S/>(s)(<REPEAT_FROM_TO_PARAMETRIC_S/>(s, from, to, macro, data))</syntax>
		</usage>
		<derivative id="REPEAT_FROM_TO_PARAMETRIC_S_ID"/>
		<derivative id="REPEAT_FROM_TO_PARAMETRIC_S_" lang="C99"/>
	</derivative>
</macro>

</header>
